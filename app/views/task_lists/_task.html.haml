%a.task_link{:href => task_list_task_path(@task_list, task)}
%span
  =task.name
%span.complex
  =task.complexity
%span.actions
  %ul.task_status
    %li
      ready?
      =check_box_tag("is_ready?")
    %li
      blocked?
      =check_box_tag("is_blocked?")
=prioritisation_links(task)