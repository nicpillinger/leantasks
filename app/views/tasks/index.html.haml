%h1 #{h @task_list.name}

%table
  - @task_list.tasks.each do |task|
    %tr
      %td= task.name
      %td= task.complexity
      %td= link_to 'details', task_list_task_path(@task_list, task)      

%br

= link_to 'Add a new task', new_task_list_task_path(@task_list)
